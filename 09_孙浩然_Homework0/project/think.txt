vector 是一个动态数组，没有固定长度 const vector<double>& cof  和  const double* cof 基本等价？
std::list 是一个双向链表有一系列method，std::list<Term> m_Polynomial;本身是无序的
std::map 是一个映射，内有很多组对应的元素，可以利用不同的类型去查找，返回 std::map<int, double> m_Polynomial; 但要注意的是map只能选择int作为key值进行索引
